version: "3.3"

services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./:/app
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    links:
      - app

  app:
    image: phpgt/php:7.2-fpm
#    build: .
    volumes:
      - ./:/app